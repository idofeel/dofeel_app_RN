{
	"name": "app",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"start": "node node_modules/react-native/local-cli/cli.js start",
		"test": "jest",
		"ios": "react-native run-ios",
		"iosx": "react-native run-iosx",
		"build": "rm -rf bundle && mkdir bundle && react-native bundle --entry-file index.js --bundle-output ./bundle/index.android.bundle --platform android --assets-dest ./bundle --dev false && react-native bundle --entry-file index.js --bundle-output ./bundle/index.ios.bundle --platform ios --assets-dest ./bundle --dev false "
	},
	"dependencies": {
		"eslint": "^5.15.0",
		"react": "16.6.1",
		"react-native": "0.70.2",
		"react-native-gesture-handler": "^1.0.17",
		"react-navigation": "^3.3.2"
	},
	"devDependencies": {
		"babel-jest": "23.6.0",
		"jest": "23.6.0",
		"metro-react-native-babel-preset": "0.50.0",
		"react-test-renderer": "16.6.1"
	},
	"jest": {
		"preset": "react-native"
	}
}
